# 更新日志

本文档记录拾光（Glimmer）各版本的主要变更。

## [4.0] - 待发布

### 发布目标

4.0 将作为拾光面向长期使用与公开分发的成熟产品版本。发布前必须重新完成核心功能、数据可靠性、提醒与日历同步、桌面小组件、无障碍、性能、渠道构建和真实设备体验验证；未完成的项目保留在 4.0 发布检查清单中，不提前标记为已验证。

### 修复

- 修复桌面小组件选择“跟随首页”后仍固定使用自定义顺序的问题；小组件现在读取首页当前的“按天数 / 按日期 / 自定义”排序模式，并直接复用首页排序实现。
- 保证“跟随首页”时置顶事件继续按置顶顺序显示在最前；首页选择“按距离天数”后，小组件未置顶事件也按相同天数规则排列。
- 首页切换排序、自定义拖动排序或切换事件置顶状态后，立即通知桌面小组件刷新，避免顺序要等到下次启动或事件更新才生效。
- 收紧发布脚本版本解析：无法读取 `VERSION_NAME` 时直接终止，显式 tag 与项目版本不一致时拒绝发布，避免误更新旧版 Release。
- 发布脚本验证正式签名指纹与本地/远端 tag commit，以 Git ref 锁串行发布；只恢复带脚本 ownership marker 的 draft，拒绝覆盖 published Release 或接管人工 draft。
- GitHub Release publisher 会删除 owned draft 中的所有旧资产，只接收 exact Direct APK，并在发布前后要求整个 Release 只保留唯一的 exact Direct APK，绑定资产 id、size、digest 与下载 URL；Play AAB 只交付 Play Console。
- 明确 4.0 发布顺序：推送最终不可变 tag 后从对应 commit 新鲜正式签名构建，不复用旧产物；本地通过 `gh auth login` / `gh auth token` 获取凭据，CI 才注入 secret。
- 修复深色首页事件文字、月历选中日期的颜色对比，并补齐滚轮、设置单选组和空状态的无障碍语义与 48dp 触控目标。
- 修复应用语言与系统语言不同时，保存失败或“无可写系统日历”提示可能混入系统语言的问题；提示资源现在由当前界面语言环境解析。
- 将分享卡渲染与图片写入移出主线程；启动阶段的语言偏好读取明确使用 IO 调度器，同时保留首帧前应用语言的正确性。

### 测试

- 根据 3.17 手机导出的 22 条真实事件结构新增脱敏回归 fixture，保留 ID、日期、分类、重复类型、农历标记与创建时间，同时移除私人标题。
- 新增首页与小组件排序结果一致性断言，并单独验证置顶事件始终位于列表最前。
- 新增发布契约测试，覆盖签名门、不可变 tag、draft ownership、并发锁、资产身份与最终公开状态验证。
- 恢复已发布 v3.4 的真实 Room v6 schema，并在 API 37 验证完整 6→7→8→9→10 迁移链与数据保留。
- Direct / Play JVM 各 409 项通过；五项 lint / vital lint 均为零问题，并新增 pull request 双渠道质量门。

### 版本信息

- `versionName`: `4.0`
- `versionCode`: `23`
- Direct APK 目标文件名：`glimmer-countdown-4-0.apk`
- 发布状态：未发布；最新公开版本仍为 3.17

## [3.17] - 2026-07-08

### 发布摘要

3.17 是一次面向公开分发的稳定版更新，重点收口桌面小组件体验、设置层级、启动流程和新建事件输入链路。这个版本把小组件从主题设置里独立出来，并把桌面小组件的视觉效果向系统原生小组件靠拢。

### 亮点

- 独立“小组件设置”入口：默认配置、已有小组件、字号和内容规则集中管理，主题页回到主题、颜色、字体和应用字号本身。
- 更柔和的小组件视觉：25% / 50% / 75% 玻璃背景改为乳白磨砂层，弱化硬边线，优化圆角、正文颜色和强调色。
- 更明确的尺寸配置：宽高设置改为“预览宽度 / 预览高度”，支持 1-5 格预览配置，并明确不会改写 Launcher 桌面物理尺寸。
- 已有小组件复用新增配置页：编辑入口不再在列表内展开长表单，配置路径更短、更清楚。
- 默认配置立即生效：默认小组件配置变更后会刷新桌面小组件，并提供“应用到已有小组件”操作。
- 新建事件输入更可靠：修复标题输入焦点、拼音组合态和硬件键盘模式下 IME 不显示的问题。
- 系统日历策略更清楚：只检查设备已有可写日历；没有可写日历时给出明确提示，不自动创建“拾光”本地日历。
- 启动体验更干净：移除应用内过渡页，只保留系统启动页，避免启动内容重复闪现。

### 版本信息

- `versionName`: `3.17`
- `versionCode`: `22`
- Direct APK: `glimmer-countdown-3-17.apk`

### 验证

- `testDirectDebugUnitTest`
- `testPlayDebugUnitTest`
- `compileDirectDebugAndroidTestKotlin`
- `lintDirectDebug lintDirectRelease lintPlayRelease lintVitalDirectRelease lintVitalPlayRelease`
- `assembleDirectRelease assemblePlayRelease bundlePlayRelease`
- Direct release APK 模拟器 smoke：启动首页、小组件设置、新建标题输入、无可写系统日历提示与保存回首页

## [3.16] - 2026-07-06

### 变更

- 升级应用版本至 `3.16`（`versionCode=21`）
- 首页“今日 / 七日 / 本月 / 节点”改为右上操作菜单中的近期入口，避免常驻占用首屏
- 列表视图收敛为桌面小组件式紧凑行，只保留事件名称与时间核心信息
- 卡片视图补齐分类、重复、提醒等辅助行，保持纸笺信息层级
- 月历移除“本月重点”，点击日期后直接展示该日期的事件、农历和相对时间；点击顶部年月可切换月份
- 新建 / 编辑页合并事件模板与分类，日期卡片前置，提醒提前天数与提醒时间统一为宋式滚轮弹窗
- 详情页主卡轻量化，仅保留分类、日期、标题和核心倒计；农历、生日、重复、提醒、备注与警告移到下方无框表格区
- 详情页底部操作改为无外框四等分：置顶、编辑、分享、删除
- 新增宋式分享卡预览，支持保存图片和调起 Android 原生分享面板
- 设置页显示分组改为语言、首页显示、日期格式三段折叠纸笺
- 默认提醒和节点提醒提前天数提供常用预设，减少大范围滚轮操作

### 修复

- 修复详情页提醒状态 CTA 文案与实际跳转不一致的问题，通知 / 日历权限问题会进入对应系统设置
- 修复通知深链在设置页或编辑页收到事件 ID 时可能不回到 Home 展示详情的问题
- 优化 `ClassicalToggle` 的触控目标和 Switch 无障碍状态
- 优化首页右侧时间切换区域的无障碍说明
- 优化月历选中日期事件列表在小屏和大字体下的剩余空间分配
- 优化新增 / 编辑页日期卡片文案层级，去除重复“日期”标签并突出具体日期
- 清理 3.15 移除功能留下的未使用资源，并将英文数量文案改为 plurals

### 验证

- `testDirectDebugUnitTest`
- `testPlayDebugUnitTest`
- `compileDirectDebugAndroidTestKotlin`
- `lintDirectDebug`
- `lintDirectRelease`
- `lintPlayRelease`
- `lintVitalDirectRelease`
- `lintVitalPlayRelease`
- `assembleDirectRelease`
- `assemblePlayRelease`
- `bundlePlayRelease`

## [3.15] - 2026-07-02

### 变更

- 升级应用版本至 `3.15`（`versionCode=20`）
- 首页新增“今日 / 七日 / 本月 / 节点”近期摘要，支持按摘要聚焦对应事件集合
- 月历新增“本月重点”，按生日、纪念日、倒数日和重大节点聚合当前月事件
- 新增提醒可信度模型，统一表达应用提醒、通知权限、系统日程权限、可写日历和同步错误状态
- 详情页新增提醒可信度状态条，并将底部操作收口为统一宋式工具栏
- 新增 / 编辑事件页加入实时预览卡，帮助用户在保存前确认标题、分类、日期、颜色和提醒状态
- 新增生日、纪念日、倒数日模板，自动套用分类与重复规则默认值
- 颜色预设升级为命名宋式色板，包含墨、黛蓝、松绿、青瓷、朱砂、赭石、旧金、茶褐和藕荷
- 设置页外观区新增样张预览，系统日程同步区新增提醒与日程健康状态
- 启动页缩短动画节奏并加入“拾光”品牌字标，保留 Android themed icon 单色资源检查

### 修复

- 当事件主体已保存但设备没有可写系统日历时，保存页会返回上一层并以状态条 / 提示说明同步不可用，避免用户误判为保存失败

### 验证

- `HomeTimelineModelsTest`
- `HomeCalendarModelTest`
- `ReminderStatusModelsTest`
- `EventTemplateModelsTest`
- `HomeInteractionPolicyTest`
- `SettingsStructureTest`
- `ReleaseReadinessTest`

## [3.14] - 2026-06-30

### 变更

- 升级应用版本至 `3.14`（`versionCode=19`）
- 优化首页月历日格布局：移除每个日期格内的农历文本，避免实机窄屏显示为“四...”等截断残片
- 月历日格改为显示公历日期、今日标记和事件点/数量；完整农历日期移动到选中日期区域展示
- 优化小组件透明 / 半透明背景：透明自动对比跟随系统深浅色，半透明背景改为分档玻璃质感层叠资源，并为透明 / 玻璃模式增加文字阴影保护
- 优化重大节点识别：生日、纪念日和普通倒计时改用类型化候选与评分策略，优先突出半岁 / 周年 / 百日 / 倒计时阈值等更有语义的节点
- 首页顶部整合搜索、筛选、排序和设置入口；搜索展开时替换标题区域，内容区减少独立工具按钮占位
- 首页“卡片 / 列表 / 月历”切换改为轻量宋式页签，降低首屏控制区占用
- 调整浅色主题卡片纸色：由纯白改为暖宣纸色，过期卡片使用更柔和的旧纸色，并降低边框与色线强度
- 拉开卡片、列表、月历三种视图层级：卡片保持册页 / 笺纸感，列表改为透明书目行与极细分隔线，月历格子与选中事件行改为更轻的历书样式

## [3.13] - 2026-06-29

### 变更

- 升级应用版本至 `3.13`（`versionCode=18`）
- 桌面小组件新增 2x2、3x3、4x2 尺寸模板，支持透明、半透明、宣纸、青瓷、朱印等背景风格
- 小组件支持按全部事件、置顶、未来、生日筛选，并可配置首页排序、置顶优先或最近优先
- 小组件新增紧凑、标准、宽松密度，支持边框、圆角、文字对比、农历前缀与独立字号设置
- 新增小组件配置 Activity，添加小组件时可配置；设置页支持管理默认配置并应用到已有小组件
- 设置页改为分类入口 + 折叠分组结构，字体、字号、小组件、里程碑、提醒、日程同步等细节不再平铺
- 内置 Noto Serif SC 与 ZCOOL XiaoWei 字体，默认使用真实宋体主视觉，并保留系统黑体、系统衬线与默认字体选项
- Play 渠道移除直接 APK 安装权限，GitHub Release 仅作为 Direct 渠道应用内更新来源
- GitHub Release 资产只上传 Direct APK，避免 Direct 应用内更新误下载 Play 包

### 修复

- 修复字体默认值与首帧初始值不一致导致的启动字体闪动风险
- 修复设置页日程同步区块折叠时仍立即刷新同步状态的问题
- 补充设置折叠区的无障碍 button role、展开 / 折叠状态和图标说明
- 移除完整 Noto Sans SC 随包字体，将 Release APK 体积从约 36M 收口到约 25M

### 验证

- `testDirectDebugUnitTest`
- `testPlayDebugUnitTest`
- `compileDirectDebugAndroidTestKotlin`
- `lintDirectRelease`
- `lintPlayRelease`
- `assembleDirectRelease`
- `assemblePlayRelease`
- `bundlePlayRelease`
- Direct / Play APK 签名与权限核验

## [3.12] - 2026-06-24

### 变更

- 升级应用版本至 `3.12`（`versionCode=17`）
- 新增“记得日子” `.mdb` 备份文件导入适配，可从旧 Realm 备份中恢复事件标题、日期、备注与创建时间
- 文件导入入口从仅限文本文件放宽为通用文件选择，继续兼容 JSON 备份导入与文本粘贴导入
- 导入流程改为先预览再确认，展示识别数量、可导入数量、重复跳过数量与解析失败数量
- 旧 `.mdb` 备份导入会按源 `oid` 去重，并在写入前跳过应用内已存在的同名同日同分类事件
- 增加脱敏“记得日子”备份 fixture 与解析回归测试，覆盖生日、纪念日、备注和重复记录映射
- 月历视图改用独立未截断数据源，按当前月份展开一次性、每周、每月、每半年、每年和农历每年重复事件
- 修复月底每月重复事件在经历短月份后后续月份错位的问题，例如 1 月 31 日在 3 月仍显示为 3 月 31 日
- 删除事件增加“已删除 / 撤销”提示，并为已删除或不存在的详情入口显示明确空状态
- 首页视图切换改为“卡片 / 列表 / 月历”分段控件，自定义排序增加长按拖动提示
- 编辑页保存按钮固定到底部，农历日期选择支持输入年份跳转，颜色选择触控区扩大并补充无障碍描述
- JSON / CSV 导出新增“保存为文件”入口，设置子页面支持旋转后保持当前分类
- 清理详情页旧按钮实现、未使用滑动提示偏好项与明显乱码注释，降低后续维护成本

## [3.11] - 2026-06-01

### 变更

- 升级应用版本至 `3.11`（`versionCode=16`）
- 新增“新建事件默认提醒”设置，支持分别配置默认提醒开关、提前天数与提醒时间
- 新建普通事件时改为读取用户默认提醒配置；编辑已有事件时保持原数据，不做覆盖
- 将默认值调整为“提醒开启、提前 7 天、10:00”
- 设置入口加入“里程碑与提醒”页，并保持现有主题适配与交互风格一致

### 修复

- 修复 `previousOccurrenceDate` 对每周/每月/每半年重复事件返回未来日期的问题，导致“已过去天数”始终显示 0
- 修复首页列表项文字颜色在暗色模式下不自适应的问题
- 修复编辑页 `lateinit var` 在 Compose 重组竞态下可能触发 `UninitializedPropertyAccessException` 崩溃的问题
- 修复 JSON 导入在遇到格式错误时静默丢弃全部事件的问题，改为逐条解析并报告错误数
- 修复详情页删除确认使用 `delay(200)` 导致旋转屏幕后可能卡在已删除事件页面的问题
- 修复详情页事件加载失败时显示无限 spinner 且无法返回的问题，添加返回按钮
- 修复里程碑模式下无即将到来节点时错误显示“0 天”的问题，改为显示“无即将到来的节点”
- 修复 CSV 导出丢失 colorHex、remindDaysBefore、reminderTimeMinutesOfDay 等 6 个字段的问题
- 修复纯文本导出对今天事件显示“已经 0 天”的问题
- 修复设置按钮、操作栏按钮、颜色选择器触控目标不达标的问题
- 修复里程碑通知与倒计时通知共享同一 channel 导致用户无法分别配置的问题
- 修复里程碑通知 ID 与倒计时通知 ID 在 eventId≥1000 时可能碰撞的问题
- 修复 `RescheduleAllWorker` 在快速连续修改设置时反复重启的问题，添加 500ms 防抖
- 修复 `GitHubReleaseUpdateChecker` 每次调用创建新 OkHttpClient 的问题，改为单例复用
- 修复 `GitHubReleaseUpdateChecker` 中 `resp.body` 未做 null 检查的问题
- 为数据库迁移 5→6 和 8→9 添加注释，说明 `tags` 和 `birthHour`/`birthMinute` 列被有意移除

### 验证

- `assembleDirectDebug`
- `testDirectDebugUnitTest`
- `lintDirectDebug`
- `assembleDirectRelease`
- `bundlePlayRelease`
- `EventEntryValidationTest`
- `ReminderDateCalculatorEdgeTest`
- `ScheduleSyncManagerTest`

## [3.10] - 2026-04-10

### 修复

- 升级应用版本至 `3.10`（`versionCode=15`）
- 修复用户设置“提前 X 天提醒”并同步到系统日程后，兼容性回退链路仍只写入第 X 天单条提醒的问题
- 将系统日程兼容性回退从旧的单条 `Events + Reminders` 写入，补齐为从第 X 天到当天的整段提醒系列写入
- 保持“当天提醒”的重复事件 RRULE 同步策略不变，避免对周 / 月 / 年重复场景引入额外回归

## [3.9] - 2026-04-07

### 修复

- 升级应用版本至 `3.9`（`versionCode=14`）
- 将系统日程同步恢复为更接近 `3.6 / 3.7` 的“先真实写入，再根据结果判定”的策略，移除保存前对标准可写日历账户的强依赖
- 修复部分系统日历 Provider 可以写入事件，但不兼容新版系列同步查询 / 更新 / 清理流程时，保存后仍被误判为“部分提醒/同步操作失败”的问题
- 当新版系列同步链路失败或未实际插入任何系统日程时，自动回退到旧版单条 `Events + Reminders` 写入路径
- 将系统日历扩展属性与 `Events.DESCRIPTION LIKE` 清理查询的兼容性问题降级为安全日志，不再因为清理旧提醒或旧里程碑失败而误报保存失败
- 为保存链路补充更明确的日志，便于后续在 logcat 中区分应用提醒、系统日历同步、里程碑同步和小组件刷新失败点

## [3.8.1] - 2026-04-03

### 修复

- 升级应用版本至 `3.8.1`（`versionCode=13`）
- 修复在系统设置中已授予通知和日历权限，但设备没有可写日历时，保存事件仍错误提示“部分提醒/同步操作失败”的问题
- 新增保存前、权限回调后和同步开关开启时的可写日历检查，避免无效同步请求继续落到通用失败提示
- 当系统中没有可写日历时，改为明确提示用户前往系统同步设置，或以“不同步日历”的方式继续保存
- 修复同步开关在无可写日历场景下仍可被开启的问题

## [3.8] - 2026-04-03

### 变更

- 升级应用版本至 `3.8`（`versionCode=12`）
- 小组件改为跟随系统主题，补齐动态配色、背景、圆角与字体缩放策略
- 统一小组件刷新链路，主题切换、尺寸变化与内容更新时更加稳定
- 设置首页视觉改为细线分隔结构，收敛卡片化边框

### 修复

- 修复权限申请链路中通知能力判断不完整的问题
- 修复权限不足时保存事件仍停留在编辑页的问题
- 修复权限弹窗关闭手势可能误触发降级保存的问题
- 修复编辑页误触返回直接丢失未保存内容的问题
- 修复非法日期输入被静默回滚的问题
- 修复首页拖拽排序容易被后台列表刷新打断的问题
- 修复详情页底部操作区在小屏设备上拥挤的问题
- 修复详情页保留分享按钮导致操作区过长的问题
- 修复桌面小组件每条事件存在多余独立外框的问题
- 修复小组件点按事件在部分系统上无法准确打开对应详情的问题

## [3.7] - 2026-04-01

### 变更

- 升级应用版本至 `3.7`（`versionCode=11`）
- 重构桌面小组件主题解析与刷新链路
- 恢复桌面小组件可滚动列表能力，重新支持显示全部事件
- 同步更新 README、发布文档与脚本到 `3.7`

### 修复

- 修复系统深浅色切换后，小组件偶发停留在旧主题的问题
- 修复应用内主题切换过程中，小组件偶发空白或不显示内容的问题
- 修复界面重建后小组件刷新可能丢失的问题
- 修复 `3.7` 首发后小组件只显示部分事件的问题

## [3.6] - 2026-03-17

- 限制农历事件重复规则为“不重复 / 每年重复”，保证编辑页与实际行为一致
- 首页默认排序改为“自定义排序”，并仅在该模式下允许拖拽
- 修复系统日历同步误删里程碑日程的问题
- 修复未来事件里程碑提醒可能漏提醒的问题

## [3.5] - 2026-03-11

- 新增统一提醒重建链路，覆盖开机、时区变化、手动改时与冷启动恢复场景
- 新增提醒时间与提前天数滚轮选择
- 重构设置页信息架构与事件编辑顺序

## [3.4] - 2026-03-10

- 新增首页搜索、组合筛选与月历视图
- 新增全局基础字号缩放与小组件独立字号缩放
- 新增里程碑提醒时间配置与导入后自动重建

## [3.3] - 2026-03-04

- 上线系统日历同步开关与基础同步能力
- 对齐小组件排序语义与首页排序语义
- 完成第一轮小组件文案与布局压缩优化

## [3.2] - 2026-03-03

- 优化日期选择器的公历 / 农历切换与滚轮交互体验
- 优化首页视图切换与小屏适配

## [3.1] - 2026-03-02

- 完成农历事件的存储、计算、展示与提醒链路支持
- 首页与小组件增加农历标识能力

## [3.0] - 2025-02-28

- 重构主题与整体视觉体系
- 优化首页与详情页交互稳定性

## [2.0] - 2025-02-27

- 引入应用内更新能力预留与小组件体验增强

## [1.0] - 2025-02-26

- 初始版本发布，提供事件管理、提醒通知、主题与小组件能力
