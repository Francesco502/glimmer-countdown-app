# 更新日志

本文档记录拾光 (Glimmer) 各版本的更新内容。应用内「检查更新」弹窗中的「更新说明」来自 GitHub Release 的 body，可与本文件对应版本一致。

---

## [3.1] - 2026-03-02

### 农历事件全链路支持

- **农历事件标记与存储**：在 `Event` 实体中新增 `isLunar` 字段，通过 Room Migration 平滑升级老数据；底层依然统一以公历 UTC 午夜毫秒数存储时间，仅在展示与重复计算时按农历语义处理。
- **农历核心计算工具**：新增 `LunarEventUtils`，基于 `cn.6tail:lunar` 实现 `getNextLunarOccurrence` / `getLunarElapsedPeriod` 等函数，准确计算农历事件的下一次发生日与已历年数。
- **详情页农历展示升级**：纪念日详情中的「缘起｜静候」与生日详情中的农历信息统一使用「岁次 甲申 腊月 初八」等格式展示，兼顾干支纪年与农历月日阅读习惯。
- **首页与小组件农历标识**：对于标记为农历的事件，首页与桌面小组件会增加 `[农]` 标签与对应农历文案，避免与公历事件混淆。
- **提醒调度对农历友好**：重复事件的提醒时间在每次触发后按农历重新计算下一次的实际公历日期，而不是简单地公历加一年，确保农历生日与节日的提醒始终准确。

### 首页与小组件体验优化

- **首页分类重构**：顶部筛选改为「全部 / 生日 / 纪念日 / 其他」四类，删除「即将到来 / 已过期」分类，让事件管理更加贴合真实使用场景。
- **排序体验**：保留首页右上角排序按钮，支持按剩余天数、目标日期和创建时间排序，默认按剩余天数排序，更符合倒计时直觉。
- **桌面小组件排版优化**：整体字号略微缩小，标题与天数信息更加紧凑，不会占据过多桌面空间。
- **修复小组件“已经...”显示异常**：现在小组件列表会稳定显示「事件标题 + 纯数字天数」，状态标签与天数不再互相干扰，避免出现整行似乎都变成“已经…”的错觉。
- **日期选择器增强**：重构底部弹窗日期选择器，支持年 / 月 / 日三级滚轮、中心吸附与「公历 / 农历」模式切换，并在农历模式下以宋式美学样式展示农历年月日。

---

## [3.0] - 2025-02-28

### 重要修复与发布就绪

- **宋代美学重构**：默认卡片色重新调整为具有东方古典韵味的颜色（沉香、景泰蓝、汁绿、丹罽、秋香、栗壳、蟹壳青、铁灰、绛紫），提升饱和度使界面更清透。
- **色彩对比度增强**：详情页卡片底色根据所选主题色自动进行色彩融合，并增加卡片阴影，浅色模式像染色宣纸，深色模式像有底色的墨锭，彻底解决背景死白刺眼或死黑无法区分的问题。
- **命理历法升级**：彻底弃用“纳音五行”，重构为最正统、主流的“子平八字法（正五行+地支藏干）”，现在会直接显示日主本命（如“属木 (缺水)”或“属火 (五行俱全)”），展示更准确。
- **视觉优化**：适当调大详情页“缘起｜已历｜静候”的字号并加深颜色透明度；启动页移除不需要的元素，将主图（Logo）尺寸扩大为240dp以强化品牌感。
- **首页体验与详情覆盖层**：
  - 详情页改为在首页之上的覆盖层展示，从首页进入详情再返回时，列表保持原滚动位置不再自动滑到顶部/底部。
  - 支持系统返回键优先关闭详情覆盖层，而不是直接退出首页或应用。
  - 旋转屏幕后会自动恢复当前正在查看的详情，不会因为配置变更而丢失页面。
  - 调整首页卡片与列表的点击区域，右侧时间区域用于切换“天数 / 年月日”显示，其余区域整体点击即可进入详情，消除“点按无响应”的死区。

---

## [2.1] - 2025-02-28

### 改进

- **宋代美学主题**：首页卡片默认配色改为淡雅绢本设色风格（秋香、退红、雨过天青、绢色、蟹壳青、月白、藕荷、淡墨、檀色）
- **启动页 Slogan**：增加了符合宋代美学的启动页 Slogan：“在时间的深海里，拾起一缕微光。”
- **卡片颜色自定义**：新建/编辑事件时支持自定义十六进制颜色，可通过调色板按钮输入任意颜色
- **绢本质感优化**：卡片透明度调整，更贴合绢本设色质感

---

## [2.0] - 2025-02-27

### 新增

- **应用内检查更新**：支持从 GitHub Release 检测新版本并下载安装
- **桌面小组件优化**：
  - 事件增删改后即时刷新
  - 已过 / 临近（7 天内）标签提示
  - 列表项圆角背景
  - 按天轮询（原为每小时）

### 改进

- **图标**：沙漏整体 0.7 倍缩放，顶底弧线修平为接近平直线
- **字体**：补全 14 个 Typography 样式，所有界面统一跟随设置中的字体（默认 / 衬线 / 手写 / 等宽 / 瘦金体）
- **顶部栏**：「拾光」标题随字体设置展示（如瘦金体）
- **小组件点击**：应用已打开时从小组件点击事件可正确跳转详情（`singleTop` + `onNewIntent`）

### 技术

- targetSdk 35
- 多渠道：direct（直装）、play（Play 商店）

---

## [1.0] - 初始版本

- 倒计时 / 纪念日管理
- 多语言（中 / 英）
- 主题切换（浅色 / 深色 / 跟随系统）
- 桌面小组件
- 提醒通知
- 导出 / 导入 JSON
